<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failure</title>
    <link href="https://fonts.googleapis.com/css?family=Nunito+Sans:400,400i,700,900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            text-align: center;
            padding: 40px 0;
            background: #EBF0F5;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .card {
            background: white;
            padding: 3rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            display: inline-block;
            width: 90%;
            max-width: 400px;
            text-align: center;
        }

        .icon-container {
            border-radius: 50%;
            height: 150px;
            width: 150px;
            background: #F8FAF5;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }

        .icon-container i {
            color: #E05263;
            font-size: 80px;
        }

        h1 {
            color: #E05263;
            font-family: "Nunito Sans", "Helvetica Neue", sans-serif;
            font-weight: 900;
            font-size: 2rem;
            margin-bottom: 0.8rem;
        }

        p {
            color: #404F5E;
            font-family: "Nunito Sans", "Helvetica Neue", sans-serif;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
        }

        .button-container {
            margin-top: 1.5rem;
        }

        .button {
            background-color: #e60000;
            color: white;
            border: none;
            padding: 12px 20px;
            font-size: 1rem;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            max-width: 200px;
        }

        .button:hover {
            background-color: #cc0000;
            transform: translateY(-2px);
        }

        @media (max-width: 480px) {
            .card {
                padding: 2rem;
            }

            h1 {
                font-size: 1.8rem;
            }

            p {
                font-size: 1rem;
            }

            .icon-container {
                height: 120px;
                width: 120px;
            }

            .icon-container i {
                font-size: 60px;
            }

            .button {
                font-size: 0.9rem;
                padding: 10px;
            }
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="icon-container">
            <i>✘</i> <!-- Failure Icon -->
        </div>
        <h1>Payment Failed</h1>
        <p>Your payment was unsuccessful.<br /> Please try again.</p>
        <div class="button-container">
            <button class="button" onclick="navigateToUrl()">Retry</button>
        </div>
    </div>

    <script>
        function navigateToUrl() {
            window.location.href = "/"; 
            // localStorage.removeItem("cart")
        }
    </script>
</body>

</html>
